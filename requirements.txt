# ================================================
# ОБЯЗАТЕЛЬНЫЕ (standalone режим, CPU или GPU)
# ================================================

# PyTorch (CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu)
# PyTorch (CUDA 12.4: pip install torch --index-url https://download.pytorch.org/whl/cu124)
torch>=2.4.0

# HuggingFace stack
transformers>=4.45.0
datasets>=2.20.0
huggingface-hub>=0.24.0
tokenizers>=0.19.0
accelerate>=0.33.0
peft>=0.12.0

# Knapsack RL (numba-ускоренный DP солвер)
numba>=0.59.0
numpy>=1.24.0

# Metrics & logging
nltk>=3.8.0
sacrebleu>=2.4.0
wandb>=0.17.0
tensorboard>=2.17.0

# Data
jsonlines>=4.0.0
tqdm>=4.66.0
pyarrow>=15.0.0

# Config
pyyaml>=6.0.0
omegaconf>=2.3.0
antlr4-python3-runtime==4.9.3
hydra-core>=1.3.0

# Build tools (нужны до flash-attn)
psutil
ninja
packaging

# Dev
pytest>=7.0.0

# ================================================
# ОПЦИОНАЛЬНЫЕ (для distributed / GPU ускорения)
# ================================================

# Flash Attention 2 (GPU only, CUDA required)
# pip install flash-attn --no-build-isolation
# Или готовый wheel:
# pip install https://github.com/Dao-AILab/flash-attention/releases/download/v2.8.3/flash_attn-2.8.3+cu124torch2.5-cp311-cp311-linux_x86_64.whl

# verl distributed GRPO framework (optional)
# pip install verl>=0.3.0 vllm>=0.8.2 "ray[default]>=2.35.0"
